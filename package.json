{
  "name": "mustview-housing",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Mobile-first Korean public rental housing notice dashboard powered by official MyHome and LH data.",
  "scripts": {
    "check": "node --check functions/api/coupang-partners.js && node --check coupang-partners.js && node scripts/run-js-checks.mjs && astro check",
    "test": "node --test --test-concurrency=1 \"test/**/*.test.js\"",
    "lint": "npm run check",
    "build": "node scripts/check-image-alt.js && node scripts/sync-astro-public.mjs && astro build && node scripts/validate-astro-output.mjs && node scripts/compare-astro-output.mjs",
    "start": "astro dev --host 127.0.0.1",
    "serve": "npm run build && npx wrangler pages dev dist"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "astro": "^7.2.10",
    "typescript": "^6.0.3",
    "wrangler": "latest"
  }
}
